setupRequired(afw >= 5.0.1.0)
setupRequired(meas_algorithms >= 5.0.1.0)
setupRequired(pex_logging >= 4.7.3.0)
setupRequired(pex_config)
setupRequired(pipe_base >= 5.0.0.1)

setupRequired(wcslib >= 4.4.4)
setupRequired(astrometry_net == 0.40)
setupRequired(numpy)

setupOptional(matplotlib)

envPrepend(PYTHONPATH, ${PRODUCT_DIR}/python)
envPrepend(LD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
envPrepend(DYLD_LIBRARY_PATH, ${PRODUCT_DIR}/lib)
